spring.application.name=protel_backend

# Koneksi Database
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/db_medisync}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:1234}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JPA / Hibernate Settings
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

server.address=0.0.0.0
server.port=8080
spring.devtools.restart.enabled=false

# CONFIGURATION SUPABASE STORAGE
supabase.url=https://rxijtezzwynrhwqanhsa.supabase.co
supabase.key=${SUPABASE_KEY}
supabase.bucket=profile-images